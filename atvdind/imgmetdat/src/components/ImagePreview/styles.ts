import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 6,
  },
  image: {
    width: "100%",
    height: 200,
    borderWidth: 3,
    borderColor: "red",
    borderRadius: 8,
  },
  caption: {
    color: "#333",
  },
});
